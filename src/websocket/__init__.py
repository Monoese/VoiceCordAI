"""
DEPRECATED: This package (`src.websocket`) is deprecated and no longer in use.

The application has transitioned to using the `openai` Python library for
real-time API interactions, managed by the `src.openai_adapter` package.
This `src.websocket` package is retained temporarily during the transition
but should not be used for new development and will be removed in a future update.
"""
